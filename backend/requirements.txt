# ---- Core ----
Django==4.2.13
djangorestframework==3.15.2
django-cors-headers==4.4.0
python-dotenv==1.0.1

# ---- WSGI / static ----
gunicorn==22.0.0
whitenoise==6.7.0

# ---- Database ----
psycopg2-binary==2.9.9
dj-database-url==2.2.0

# ---- Cache ----
redis==5.0.7

# ---- Object storage (MinIO / S3) -- replaces cloudinary ----
django-storages==1.14.4
boto3==1.34.144

# ---- Utilities ----
Pillow==10.4.0
reportlab==4.2.2
requests==2.32.3
python-multipart==0.0.9
email-validator==2.2.0

# Removed on the Oracle migration:
#   django-heroku, dynoscale  -> Heroku-specific
#   cloudinary                -> replaced by MinIO + imgproxy
#   fastapi/uvicorn/sqlalchemy stack -> unused, never wired into this project
